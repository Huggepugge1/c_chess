#include "../board.h"
#include "../vector.h"

/**
 * Generate all possible moves.
 *
 * @param board The board to generate moves for
 *
 * @return A MoveVector containing all possible moves
 */
MoveVector *generate_moves(Board *board);
